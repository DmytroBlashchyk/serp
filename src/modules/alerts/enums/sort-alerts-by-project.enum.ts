export enum SortAlertsByProjectEnum {
  'alerts.createdAt' = 'date',
  'project.projectName' = 'project',
  // 'alerts.alertKeywordsCount' = 'numberOfKeywords',
  'rule.name' = 'rule',
  'trigger.threshold' = 'threshold',
}
