import { SharedLinkTypeEntity } from 'modules/shared-links/entities/shared-link-type.entity';

export const sharedLinkTypes = [
  {
    name: 'folder',
  },
  { name: 'project' },
] as SharedLinkTypeEntity[];
