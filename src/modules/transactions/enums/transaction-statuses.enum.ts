export enum TransactionStatusesEnum {
  created = 'created',
  ready = 'ready',
  updated = 'updated',
  paid = 'paid',
  completed = 'completed',
  paymentFailed = 'payment failed',
  subscriptionPaymentMethodChange = 'subscription payment method change',
}
